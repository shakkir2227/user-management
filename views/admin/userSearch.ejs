<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f8f8;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h2 {
            color: #333;
        }

        a {
            text-decoration: none;
            color: #3498db;
            font-weight: bold;
            margin-top: 20px;
            display: inline-block;
            padding: 10px 20px;
            background-color: #fff;
            border: 2px solid #3498db;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
        }

        a:hover {
            background-color: #3498db;
            color: #fff;
        }

        table {
            margin-top: 20px;
            border-collapse: collapse;
            width: 50%;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #3498db;
            color: #fff;
        }

        tr:hover {
            background-color: #f5f5f5;
        }

        a.edit,
        a.delete {
            display: inline-block;
            margin-top: 5px;
            text-decoration: none;
            color: #3498db;
            padding: 5px;
            margin-right: 5px;
            border: 1px solid #3498db;
            border-radius: 3px;
            transition: background-color 0.3s, color 0.3s;
        }

        a.edit:hover,
        a.delete:hover {
            background-color: #3498db;
            color: #fff;
        }

        form {
            margin-top: 20px;
        }

        input[type="text"] {
            padding: 10px;
            margin-right: 10px;
        }

        button {
            padding: 10px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }

        button:hover {
            background-color: #2980b9;
        }
    </style>
</head>

<body>

    <tr>
        <td>
            <%= userName.name %>
        </td>
        <td>
            <%= userName.email %>
        </td>
        <td>
            <%= userName.mobile %>
        </td>
        <td><a href="/admin/edit-user?id=<%= userName._id %>">Edit</a></td>
        <td><a href="/admin/delete-user?id=<%= userName._id %>">Delete</a></td>
        <br><br>
        <a href="/admin/dashboard">Back to Dashboard</a>
    </tr>
</body>

</html>